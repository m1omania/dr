# Перезапуск backend после добавления USE_SINGLE_PROCESS

## ✅ USE_SINGLE_PROCESS=true добавлен

Переменная окружения успешно добавлена в `.env` файл.

## Следующие шаги

### 1. Перезапустить backend

```bash
systemctl restart ux-audit-backend
```

### 2. Проверить статус

```bash
systemctl status ux-audit-backend
```

Должно быть: `active (running)`

### 3. Проверить логи

```bash
journalctl -u ux-audit-backend -n 20 --no-pager
```

Ищите в логах:
```
🔧 Использую --single-process для снижения нагрузки
```

Если видите эту строку - настройка работает!

### 4. Проверить нагрузку CPU и RAM

В панели Jino.ru проверьте:
- **CPU:** должна снизиться до 50-70% (было 100%)
- **RAM:** должна освободиться до 70-80% (было 97%)

## Ожидаемый результат

После перезапуска:
- ✅ CPU нагрузка снизится
- ✅ RAM освободится
- ✅ Backend будет работать стабильнее
- ✅ Ошибки 500/502 должны исчезнуть
- ⚠️ Обработка может быть немного медленнее, но стабильнее

## Проверка работы

1. Проверьте нагрузку CPU и RAM в панели Jino.ru
2. Попробуйте проанализировать сайт через frontend на Vercel
3. Ошибки 500/502 должны исчезнуть

## Если нагрузка все еще высокая

Если после перезапуска нагрузка все еще высокая (80%+):
1. Подождите 1-2 минуты (нагрузка может снижаться постепенно)
2. Проверьте, что в логах есть строка про `--single-process`
3. Рассмотрите увеличение ресурсов сервера в панели Jino.ru

## Дополнительная оптимизация

Если ресурсов все еще не хватает, можно:
1. Увеличить ресурсы сервера (RAM до 4GB, CPU до 2+ ядер)
2. Оптимизировать код для закрытия браузера после каждого запроса
3. Использовать более легкие настройки Puppeteer

