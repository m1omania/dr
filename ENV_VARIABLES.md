# Переменные окружения для деплоя

## 🔵 Backend (Render)

### Обязательные переменные:

```env
NODE_ENV=production
PORT=10000
DATABASE_PATH=/opt/render/project/src/database.sqlite
CORS_ORIGIN=https://your-app.vercel.app
```

### Рекомендуемые (для работы визуального анализа):

```env
HUGGINGFACE_API_KEY=hf_ваш_токен_здесь
```

Или (альтернативные названия, которые тоже работают):
```env
HF_TOKEN=hf_ваш_токен_здесь
HUGGINGFACE_TOKEN=hf_ваш_токен_здесь
```

### Опциональные:

```env
OPENAI_API_KEY=sk-ваш_ключ_здесь
```

**Примечание:** Если указаны и `HUGGINGFACE_API_KEY` и `OPENAI_API_KEY`, система будет использовать OpenAI в первую очередь, затем Hugging Face как fallback.

---

## 🟢 Frontend (Vercel)

### Обязательные переменные:

```env
NEXT_PUBLIC_API_URL=https://ux-audit-backend.onrender.com
```

**Важно:** Замените `https://ux-audit-backend.onrender.com` на реальный URL вашего backend после деплоя на Render.

---

## 📋 Полный список для копирования

### Render (Backend):

```
NODE_ENV=production
PORT=10000
DATABASE_PATH=/opt/render/project/src/database.sqlite
CORS_ORIGIN=https://your-app.vercel.app
HUGGINGFACE_API_KEY=hf_ваш_токен_здесь
```

### Vercel (Frontend):

```
NEXT_PUBLIC_API_URL=https://ux-audit-backend.onrender.com
```

---

## 🔑 Как получить токены:

### Hugging Face API Key:
1. Зайдите на https://huggingface.co
2. Зарегистрируйтесь или войдите
3. Перейдите в Settings → Access Tokens
4. Создайте новый токен (тип: Read)
5. Скопируйте токен (начинается с `hf_...`)

### OpenAI API Key (опционально):
1. Зайдите на https://platform.openai.com
2. Перейдите в API Keys
3. Создайте новый ключ
4. Скопируйте ключ (начинается с `sk-...`)

---

## ⚠️ Важные замечания:

1. **CORS_ORIGIN** должен точно совпадать с URL вашего frontend на Vercel (включая `https://`)
2. **NEXT_PUBLIC_API_URL** должен быть полным URL с `https://`
3. После деплоя frontend, обновите `CORS_ORIGIN` в Render на реальный URL
4. Не коммитьте `.env` файлы в git - они должны быть только в настройках платформ

---

## 🧪 Проверка после деплоя:

### Backend:
```bash
curl https://ux-audit-backend.onrender.com/health
```
Должен вернуть: `{"status":"ok","timestamp":"..."}`

### Frontend:
Откройте URL вашего приложения на Vercel и попробуйте проанализировать сайт.

