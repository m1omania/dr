# Различия между локальной и production средой

## 🔍 Основные причины различий

### 1. **Ограничения ресурсов на Render (Free план)**

**Локально:**
- Полный доступ к ресурсам компьютера
- Много памяти (обычно 8GB+)
- Быстрый CPU
- Быстрый интернет

**На Render (Free план):**
- Ограниченная память (512MB)
- Ограниченный CPU (shared)
- Медленнее сеть
- Может "засыпать" после 15 минут бездействия

**Последствия:**
- Медленные сайты могут не загрузиться за таймаут
- Большие страницы могут вызвать нехватку памяти
- Chrome работает медленнее из-за `--single-process`

### 2. **Переменные окружения**

**Локально:**
- Переменные из `.env` файла
- Все ключи доступны сразу

**На Render:**
- Переменные нужно устанавливать вручную в Dashboard
- Если ключ не установлен → функция не работает
- Проверьте: Settings → Environment Variables

**Что проверить:**
- ✅ `HUGGINGFACE_API_KEY` установлен?
- ✅ `CORS_ORIGIN` правильный?
- ✅ `PUPPETEER_CACHE_DIR` установлен?

### 3. **Сетевые ограничения**

**Локально:**
- Прямой доступ к интернету
- Нет ограничений на запросы

**На Render:**
- Может быть медленнее доступ к внешним сайтам
- Некоторые сайты могут блокировать запросы с Render
- Таймауты могут быть более строгими

**Решение:**
- Увеличены таймауты до 60-90 секунд
- Добавлены fallback стратегии загрузки

### 4. **Puppeteer настройки**

**Локально:**
- Chrome работает в обычном режиме
- Быстрая работа

**На Render:**
- Используется `--single-process` (ограничение ресурсов)
- Chrome работает медленнее
- Может быть нехватка памяти для больших страниц

**Решение:**
- Ограничена максимальная высота скриншота до 10000px
- Используется `clip` вместо `fullPage` для больших страниц

### 5. **Версии Node.js**

**Локально:**
- Может быть любая версия

**На Render:**
- Используется Node.js 22.16.0 (по умолчанию)
- Можно указать другую версию в `package.json`

## 🛠️ Что проверить, если не работает на сервере

### 1. Проверьте логи на Render
```
Render Dashboard → Ваш сервис → Logs
```

Ищите:
- ❌ Ошибки при запуске
- ❌ Ошибки API ключей
- ❌ Таймауты
- ❌ Нехватка памяти

### 2. Проверьте переменные окружения
```
Render Dashboard → Settings → Environment Variables
```

Должны быть:
- `NODE_ENV=production`
- `PORT=10000`
- `HUGGINGFACE_API_KEY=hf_...`
- `CORS_ORIGIN=https://your-app.vercel.app`
- `PUPPETEER_CACHE_DIR=/opt/render/project/src/backend/.local-chromium`
- `DATABASE_PATH=/opt/render/project/src/database.sqlite`

### 3. Проверьте health endpoint
```bash
curl https://design-review.onrender.com/health
```

Должен вернуть: `{"status":"ok","timestamp":"..."}`

### 4. Проверьте Chrome
В логах должно быть:
```
✅ Найден Chrome по пути: /opt/render/project/src/backend/.local-chromium/chrome/...
```

Если нет → Chrome не установился правильно

### 5. Проверьте API ключи
В логах должно быть:
```
Проверка API ключей:
  HUGGINGFACE_API_KEY: установлен
  hfToken найден: да
```

Если нет → API ключ не установлен

## 🚀 Рекомендации для production

### 1. Используйте платный план Render
- Больше памяти (1GB+)
- Быстрее CPU
- Не "засыпает"

### 2. Оптимизируйте запросы
- Используйте кеширование
- Ограничьте размер скриншотов
- Используйте таймауты

### 3. Мониторинг
- Следите за логами
- Проверяйте использование памяти
- Отслеживайте ошибки

## 📊 Сравнительная таблица

| Параметр | Локально | Render (Free) |
|----------|----------|---------------|
| Память | 8GB+ | 512MB |
| CPU | Полный | Shared |
| Сеть | Быстрая | Медленнее |
| Chrome | Обычный режим | `--single-process` |
| Таймауты | Гибкие | Строгие |
| Переменные | `.env` файл | Dashboard |
| "Засыпание" | Нет | Да (15 мин) |

## 🔧 Быстрое решение проблем

### Проблема: Таймауты
**Решение:** Увеличены таймауты до 60-90 секунд, добавлены fallback стратегии

### Проблема: Нехватка памяти
**Решение:** Ограничена высота скриншота до 10000px, используется `clip`

### Проблема: API не работает
**Решение:** Проверьте `HUGGINGFACE_API_KEY` в Render Dashboard

### Проблема: Chrome не найден
**Решение:** Проверьте `PUPPETEER_CACHE_DIR` и build команду

### Проблема: Медленная работа
**Решение:** Это нормально для Free плана, рассмотрите платный план

