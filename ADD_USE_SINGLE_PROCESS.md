# Добавление USE_SINGLE_PROCESS=true в .env

## Проблема
`nano` не установлен на сервере.

## Решение

### Вариант 1: Использовать echo (самый простой)

```bash
cd /opt/ux-audit/backend
echo 'USE_SINGLE_PROCESS=true' >> .env
```

Проверьте, что добавилось:
```bash
cat .env
```

Должна быть строка: `USE_SINGLE_PROCESS=true`

### Вариант 2: Использовать vi (обычно установлен)

```bash
cd /opt/ux-audit/backend
vi .env
```

В vi:
1. Нажмите `i` для входа в режим редактирования
2. Перейдите в конец файла (стрелка вниз или `G`)
3. Добавьте строку: `USE_SINGLE_PROCESS=true`
4. Нажмите `Esc` для выхода из режима редактирования
5. Введите `:wq` и нажмите `Enter` для сохранения и выхода

### Вариант 3: Использовать cat

```bash
cd /opt/ux-audit/backend
cat >> .env << 'EOF'
USE_SINGLE_PROCESS=true
EOF
```

## После добавления

Перезапустите backend:
```bash
systemctl restart ux-audit-backend
```

Проверьте статус:
```bash
systemctl status ux-audit-backend
```

Проверьте логи:
```bash
journalctl -u ux-audit-backend -n 20 --no-pager
```

Ищите в логах:
```
🔧 Использую --single-process для снижения нагрузки
```

## Проверка работы

1. Проверьте нагрузку CPU в панели Jino.ru - должна снизиться
2. Проверьте RAM - должна освободиться
3. Попробуйте проанализировать сайт через frontend на Vercel

## Ожидаемый результат

После добавления `USE_SINGLE_PROCESS=true`:
- ✅ CPU нагрузка снизится до 50-70%
- ✅ RAM освободится
- ✅ Ошибки 500/502 должны исчезнуть
- ⚠️ Обработка может быть немного медленнее, но стабильнее

